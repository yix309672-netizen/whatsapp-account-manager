# Cloudflare API Token（部署 Cloudflare Pages 用）
#
# 用法：把本文件复制为 .cf-token（同一目录），然后填写你真实的 token。
#   .cf-token 已在 .gitignore 里，不会被提交。
#
# 文件格式（两行，第二行可选）：
#   第一行：API Token（必需）
#   第二行：Account ID（可选；不填脚本会自动查询）
#
# Token 权限（Cloudflare 后台 → My Profile → API Tokens → Create Token → Custom token）：
#   Account → Cloudflare Pages → Edit
#   Account → Account Settings → Read        （脚本用它自动查 Account ID）
#
# 填好后执行：node scripts/cf-pages-deploy.js hotline-dist waam-web main
#
# ===== 下面两行是示例，请替换成你自己的 =====
REPLACE_WITH_YOUR_API_TOKEN
REPLACE_WITH_YOUR_ACCOUNT_ID
